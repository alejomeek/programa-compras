pandas
streamlit
xlrd
openpyxl
xlsxwriter
reportlab

# psycopg[binary] es de api/imports_process.py (funcion Python de Vercel de
# Fase 2), no del MVP Streamlit de arriba. Va aqui, no solo en
# api/requirements.txt, porque Vercel instala las dependencias Python del
# proyecto desde api/requirements.txt. Este archivo mantiene las dependencias
# del MVP y de las pruebas locales; las funciones de Vercel deben declarar
# también sus dependencias dentro de api/requirements.txt.
psycopg[binary]>=3.1
